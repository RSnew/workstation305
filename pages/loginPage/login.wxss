/* pages/login.wxss */
.background {
  width: 100%;
  height: 100%;
  background-size: cover;
  background-position: center;
  position: absolute;
  top: 0;
  left: 0;
  z-index: -1; /* 确保背景图在最底层 */
}
.content {
  position: relative;
  z-index: 1; /* 确保内容在背景图之上 */
}
